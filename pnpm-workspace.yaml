packages:
  - protocol
  - web
  - relay

# wrangler downloads and links its binaries (workerd and esbuild) on install;
# without this permission pnpm blocks its scripts and the installation fails.
allowBuilds:
  esbuild: true
  workerd: true
