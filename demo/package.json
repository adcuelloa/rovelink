{
  "name": "@rovelink/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/main.ts",
    "typecheck": "oxlint --type-aware --type-check .",
    "build": "echo 'no build needed; typecheck verifies types'",
    "test": "node --test src/*.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "26.4.0"
  }
}
