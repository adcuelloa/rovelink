---
/**
 * Small category/type label (e.g. "LAB", "SAFETY", "EXPLORER") placed at the
 * top of a page's content. Lives in content, not in a PageTitle override —
 * Black owns PageTitle unmodified. Styles in learn.css.
 */
---

<p class="rl-eyebrow"><slot /></p>
